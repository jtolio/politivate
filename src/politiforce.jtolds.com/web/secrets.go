package web

var (
	secret = []byte("a not real secret")
)

const (
	githubClientId     = "not a client id"
	githubClientSecret = "not a client secret"

	googleClientId     = "not a client id"
	googleClientSecret = "not a client secret"

	twitterClientId     = "not a client id"
	twitterClientSecret = "not a client secret"

	facebookClientId     = "not a client id"
	facebookClientSecret = "not a client secret"
)
